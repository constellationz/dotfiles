#!/usr/bin/env bash
# Install Librewolf on Debian

sudo apt-get update -y
sudo apt-get install extrepo -y
sudo extrepo enable librewolf
sudo apt-get --allow-releaseinfo-change update -y
sudo apt-get install librewolf -y
